Hi, we compared the performance of the Serial and Parallel Versions of the Algorithms in the previous lecture. So, you might be wondering, "If the parallel versions give good performance should we not use them everywhere?" This is not the case. Parallel versions of the algorithms have some overhead. The overhead is in the form of starting the threats and preparing the elements for parallel execution. [No audio] Therefore, the cost of initializing the threats and making the elements ready for parallel execution maybe too much. And in that case, sometimes, the sequential version of the algorithm maybe faster. 

Let's do one thing. We will perform a parallel sort on this dataset. We have only a hundred elements. 

Let's run and see how much time it takes. [No audio] It takes around 10 microseconds. [No audio] Let's use the sequential version [No audio] and this has taken only three microseconds. What happened here? 
The overhead of starting the threads was too much, and it had a negative impact on the performance of the parallel sort. [No audio] This would be applicable when you have small dataset. For large datasets, you might want to prefer the parallel versions. So, it is important while deciding between serial and parallel versions, you should perform tests on your application and see which algorithm fits your dataset. Let's see what happens if during processing of the elements, an exception is thrown. There is a difference between the behavior of the parallel and sequential versions. [No audio] While operating on the elements, the elements may internally invoke some functions. 

If such functions through an exception, and it is not handled, then all the parallel algorithms call terminate, and the program will terminate. This also applies if you are using the sequential execution policy on the parallel algorithms, but it is not applicable to non-parallel algorithms. If your elements can throw exceptions during their processing by the algorithms, then use the non-parallel versions instead. Note that, parallel algorithms may also throw exceptions. For their execution, they need to allocate memory and if that allocation fails, then they may throw an exception of type bad_alloc. [No audio] So, should we use parallel algorithms everywhere? Should the non-parallel versions be removed from the standard library? 

Note that, they will be still required for backward compatibility. They can work with both input and output iterators. There is a limitation with the type of iterators that can be used with parallel algorithms, and there are some algorithms that cannot be parallelized. For example, Shuffle, Binary search, Max, Min, etc. [No audio] For some of these algorithms, the overhead of parallelization is too much. Certain algorithms may require synchronization, and synchronization is expensive. It will further degrade the performance of the algorithm, and as we discussed earlier, parallelized algorithms will terminate on exceptions, but sequenced algorithms do not. 

Instead, you can catch the exception and handle it. If you have small number of elements in a dataset, then parallel operations will not give a good performance because of the overhead discussed earlier. That's all about parallel algorithms. I hope you can now decide when and how to choose between the sequence and the parallel algorithms. 
