Hi there. In this lecture, I will discuss the library function stood bind. This function was added in C 11 and is very similar to stood function. But it is called a binder because it can bind some arguments to a given function and then invoke that function. The arguments to the target function can be represented using either placeholders or they can be directly bound to specific values. The placeholders are represented using constant objects of some unspecified type. And these go in the nested name space stood placeholders. 

These constant objects begin with underscore and are numbered from one to N. The number of placeholders provided will depend on the Compiler implementation with stood bind. We can invoke a function that accepts N arguments just like a function that accepts N minus M arguments. For example, if I have a function that accepts three arguments using stood bind, I can convert it into a function that can accept either three arguments, two arguments, one or zero arguments. So you can think of stood bind as an adapter that can change the signature of a function stood bind returns a function object through which the target can be invoked this is the usage of stood bind. The first argument stood B is the callable that can be a function pointer, a function object or a Lambda expression. And then here we can bind some values as arguments to the call or you can even use placeholders. Here, here, I'm using bind with the square function. 

This indicates that three is bound as an argument to the square function stood bind will return a function object which can be invoked with zero arguments. But for the print function I have used a placeholder. So this means it returns a function object that will accept one argument. Let's understand this better with some more examples. 